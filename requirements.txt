# Core
pandas>=2.0
PyYAML>=6.0
Faker>=20.0

# Agent orchestration
langgraph>=0.2

# API + demo
fastapi>=0.110
uvicorn>=0.29
streamlit>=1.30

# Optional LLM providers (only needed for --provider openai|anthropic).
# The pipeline runs fully in deterministic rule-only mode without these.
openai>=1.0
anthropic>=0.30

# Tests
pytest>=8.0
httpx>=0.27          # FastAPI TestClient
